# Workstation x86_64 (Ubuntu). `pandoc` e' una dipendenza di sistema, non pip:
#   sudo apt install pandoc

# core
ultralytics>=8.3
torch>=2.8,<2.13
onnx>=1.16
onnxsim
onnxconverter-common        # cast fp16 sul grafo ONNX
onnxruntime-gpu             # CPU-only: onnxruntime
opencv-python-headless      # preprocessing per la calibrazione

# configurazione e orchestrazione
hydra-core>=1.3
omegaconf>=2.3
fabric>=3.2

# dati e analisi
pandas>=2.2
pyarrow>=16
numpy<2.3
matplotlib>=3.8
seaborn
tabulate                    # tabelle markdown nel report

# report
markdown
jinja2

# test
pytest>=8

# opzionali
hydra-joblib-launcher
wandb
