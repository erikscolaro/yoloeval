# Raspberry Pi 5 — venv dell'host, per le celle CPU-only.
# Il grosso (Voyager SDK, torch) vive nel container Ubuntu 22.04.

ultralytics>=8.3
hydra-core>=1.3
omegaconf>=2.3
numpy<2.3
onnxruntime               # build CPU aarch64, disponibile da PyPI
pyyaml
opencv-python-headless
